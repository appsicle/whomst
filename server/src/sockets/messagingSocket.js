var log4js = require("log4js");
var logger = log4js.getLogger("messagingSocket.js");
logger.level = "debug"; // trace, debug, info, warn, error, fatal


// all socekts regarding text and emotes
module.exports.listen = function (io, socket) {
    // socket.on('chat', function (socket) {
    //     console.log('User with socketId %s disconnected', socket.id);
    // });
};


